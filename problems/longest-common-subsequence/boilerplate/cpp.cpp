#include <iostream>
#include <string>

using namespace std;

int longestCommonSubsequence(const string& a, const string& b) {
    // TODO: implement
    return 0;
}

int main() {
    string a, b;
    getline(cin, a);
    getline(cin, b);
    cout << longestCommonSubsequence(a, b) << '\n';
    return 0;
}
