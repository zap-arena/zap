#include <stdio.h>

/* TODO: implement and return the number of ways */
long long climb_stairs(int n) {
    (void)n;
    return 0;
}

int main(void) {
    int n;
    if (scanf("%d", &n) != 1) return 0;
    printf("%lld\n", climb_stairs(n));
    return 0;
}
