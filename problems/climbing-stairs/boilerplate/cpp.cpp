#include <iostream>

using namespace std;

long long climbStairs(int n) {
    // TODO: implement
    return 0;
}

int main() {
    int n;
    if (!(cin >> n)) return 0;
    cout << climbStairs(n) << '\n';
    return 0;
}
